[project]
name = "codex-usage-monitor"
version = "2026.08.05"
description = "Unified Codex usage monitor and session archive"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
codex-usage-monitor = "codex_usage_monitor.cli:main"

[tool.pytest.ini_options]
pythonpath = [".", "src"]
