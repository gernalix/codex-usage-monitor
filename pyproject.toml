[project]
name = "codex-usage-monitor"
version = "2026.09.15"
description = "Codex quota monitoring, session archive, cost indexing, publishing, and health checks"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
